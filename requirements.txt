# Core scientific stack
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
matplotlib>=3.7

# Deep learning
torch>=2.0
torch-geometric>=2.3

# Graph utilities
networkx>=3.0
karateclub>=1.3

# NLP / Transformer models
transformers>=4.30

torch should be installed with the right CUDA version for your GPU (e.g., pip install torch==2.0.1+cu118 from PyTorch website)

torch-geometric sometimes requires extra wheels (torch-scatter, torch-sparse, etc.), but if you’re on PyTorch Geometric 2.3+, pip install torch-geometric should handle it.

transformers is needed for DNABERT feature extraction.
